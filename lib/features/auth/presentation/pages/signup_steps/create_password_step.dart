import 'package:flutter/material.dart';

import '../../widgets/registration_step_indicator.dart';

class CreatePasswordStep extends StatelessWidget {
  final PageController pageController;
  const CreatePasswordStep({super.key, required this.pageController});

  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Column(
        children: [],
      ),
    );
  }
}
